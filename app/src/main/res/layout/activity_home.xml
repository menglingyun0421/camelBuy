<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.lingyun.camelprocurementservice.homepage.HomeActivity">

    <RadioGroup
        android:id="@+id/home_rg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_alignParentBottom="true">
        <RadioButton
            android:id="@+id/home_rb_order"
            android:text="订单"
            android:drawableTop="@drawable/tab_order"
            style="@style/main_radioButton_style"/>

        <RadioButton
            android:id="@+id/home_rb_client"
            android:text="客户"
            android:drawableTop="@drawable/tab_client"
            style="@style/main_radioButton_style"/>

        <RadioButton
            android:id="@+id/home_rb_product"
            android:text="商品"
            android:drawableTop="@drawable/tab_product"
            style="@style/main_radioButton_style"/>

        <RadioButton
            android:id="@+id/home_rb_statistics"
            android:text="统计"
            android:drawableTop="@drawable/tab_statistics"
            style="@style/main_radioButton_style"/>

        <RadioButton
            android:id="@+id/home_rb_set"
            android:text="设置"
            android:drawableTop="@drawable/tab_set"
            style="@style/main_radioButton_style"/>
    </RadioGroup>

    <ImageView
        android:id="@+id/home_line"
        android:layout_above="@id/home_rg"
        android:background="@color/colorGray"
        android:layout_width="match_parent"
        android:layout_marginBottom="2dp"
        android:layout_height="1dp">

    </ImageView>

    <FrameLayout
        android:id="@+id/activity_main_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/home_line">

    </FrameLayout>

</RelativeLayout>
